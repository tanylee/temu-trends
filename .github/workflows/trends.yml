name: trends

on:
  schedule:
    - cron: "0 */3 * * *" # запуск каждые 3 часа
  workflow_dispatch:

jobs:
  trends:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1️⃣ Клонирование репозитория
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          persist-credentials: true

      # 2️⃣ Установка Node.js
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Установка зависимостей
      - name: Install dependencies
        run: |
          npm ci || npm install

      # 4️⃣ Сбор 3-месячных трендов (Pinterest + TikTok)
      - name: Collect 3-month trends
        env:
          OXYLABS_USER: ${{ secrets.OXYLABS_USER }}
          OXYLABS_PASS: ${{ secrets.OXYLABS_PASS }}
        run: node scripts/collect_trends.js

      # 5️⃣ Сбор сезонных трендов (ежегодные пики)
      - name: Collect seasonal trends
        env:
          OXYLABS_USER: ${{ secrets.OXYLABS_USER }}
          OXYLABS_PASS: ${{ secrets.OXYLABS_PASS }}
        run: node scripts/collect_seasonal.js

      # 6️⃣ Сбор региональных трендов (TikTok + Pinterest + Instagram)
      - name: Collect regional trends
        env:
          OXYLABS_USER: ${{ secrets.OXYLABS_USER }}
          OXYLABS_PASS: ${{ secrets.OXYLABS_PASS }}
        run: node scripts/collect_trends_regions.js

      # 7️⃣ Валидация Temu ссылок (если есть)
      - name: Validate Temu links
        run: node scripts/validate_temu_links.js || echo "no validate script"

      # 8️⃣ Сопоставление трендов с Temu товарами (опционально)
      - name: Map trends to Temu products
        run: node scripts/map_trends_to_temu.js || echo "no map script"

      # 9️⃣ Агрегация глобальных трендов (Top-50)
      - name: Aggregate all trends (global top 50)
        run: node scripts/aggregate_trends.js

      # 🔟 Коммит всех данных
      - name: Commit data
        uses: EndBug/add-and-commit@v9
        with:
          add: |
            data/products.json
            data/trends_raw_3m.json
            data/trends_raw_seasonal.json
            data/trends_regions.json
            data/trends_aggregated.json
          message: "chore: update all trends data (3m + seasonal + regions + aggregated)"
          push: true
          author_name: moodette-bot
          author_email: moodette-bot@users.noreply.github.com
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

      # 11️⃣ Автодеплой на Netlify
      - name: Trigger Netlify build
        if: success()
        run: |
          curl -X POST -d '{}' \
            -H "Authorization: Bearer ${{ secrets.NETLIFY_AUTH_TOKEN }}" \
            "https://api.netlify.com/api/v1/sites/${{ secrets.NETLIFY_SITE_ID }}/builds"
        continue-on-error: true

      # 12️⃣ Telegram уведомление об успехе
      - name: Notify success
        if: success()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="✅ TEMU Trends (3m + Seasonal + Regions + Aggregated Top50) обновлены и сайт redeployed"

      # 13️⃣ Telegram уведомление об ошибке
      - name: Notify failure
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="❌ Ошибка в TEMU Trends workflow. Проверь логи Actions."
